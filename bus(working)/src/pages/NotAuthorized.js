import React from 'react';

const NotAuthorized = () => (
  <div className="flex items-center justify-center min-h-screen text-2xl text-red-600 font-bold">
    You are not authorized to view this page.
  </div>
);

export default NotAuthorized;
